{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hipster-chart.serviceAccountName" . }}
  labels:
{{ include "hipster-chart.labels" . | nindent 4 }}
{{- end -}}
