apiVersion: v1
kind: Service
metadata:
  name: {{ include "hipster-chart.fullname" . }}
  labels:
    {{- include "hipster-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hipster-chart.selectorLabels" . | nindent 4 }}
