apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hipster-chart.fullname" . }}-test-connection"
  labels:
{{ include "hipster-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hipster-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
